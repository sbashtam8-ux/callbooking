<!DOCTYPE html>
<html lang="fa">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Callbooking Performance</title>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    body {
      font-family: "Vazir", sans-serif;
      background: linear-gradient(to bottom right, #006633, #00b36b);
      color: #fff;
      margin: 0;
      padding: 20px;
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
      background: rgba(255, 255, 255, 0.15);
      padding: 15px;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      overflow: hidden;
    }

    th, td {
      border: 1px solid rgba(255, 255, 255, 0.2);
      padding: 10px;
      text-align: center;
    }

    th {
      background-color: rgba(0,0,0,0.3);
    }

    input, textarea {
      width: 95%;
      padding: 8px;
      border-radius: 8px;
      border: none;
      outline: none;
    }

    textarea {
      resize: vertical;
    }

    .chart-container {
      width: 100%;
      max-width: 700px;
      margin: 30px auto;
      background: rgba(255, 255, 255, 0.15);
      padding: 15px;
      border-radius: 15px;
    }
  </style>
</head>
<body>
  <h1>Callbooking Performance</h1>

  <div class="chart-container">
    <canvas id="scoreChart"></canvas>
  </div>

  <table>
    <thead>
      <tr>
        <th>نام کارشناس</th>
        <th>نام هماهنگ‌کننده</th>
        <th>امتیاز</th>
        <th>مشکلات تماس</th>
        <th>مشکلات کارشناس</th>
        <th>فایل شنود</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><input type="text" placeholder="مثلاً علی رضایی" /></td>
        <td><input type="text" placeholder="مثلاً نرگس محمدی" /></td>
        <td><input type="number" placeholder="امتیاز" /></td>
        <td><textarea placeholder="مشکلات تماس..."></textarea></td>
        <td><textarea placeholder="مشکلات کارشناس..."></textarea></td>
        <td><input type="file" /></td>
      </tr>
    </tbody>
  </table>

  <script>
    const ctx = document.getElementById("scoreChart").getContext("2d");
    new Chart(ctx, {
      type: "bar",
      data: {
        labels: ["علی", "رضا", "سارا", "نگار", "مهدی"],
        datasets: [{
          label: "امتیاز کارشناسان",
          data: [85, 60, 45, 70, 50],
          backgroundColor: (ctx) => ctx.raw < 60 ? "#ff0000" : "#00cc66",
        }]
      },
      options: {
        responsive: true,
        plugins: { legend: { display: false } },
        scales: { y: { beginAtZero: true } }
      }
    });
  </script>
</body>
</html>
